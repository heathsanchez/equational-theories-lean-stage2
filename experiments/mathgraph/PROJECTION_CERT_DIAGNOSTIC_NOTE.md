# Projection certificate diagnostic

Temporary experiment note. The diagnostic workflow captures the exact Lean certificate emitted by the generic source-only closure route for the two fresh Order-5 residuals and the complete official judge response. No benchmark IDs or equation IDs are added to the solver itself; IDs only select the diagnostic rows in the workflow.
