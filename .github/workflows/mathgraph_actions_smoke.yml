name: MathGraph Actions Smoke

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/research-triggers/actions-smoke.txt

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner start
        run: echo "mathgraph-actions-smoke-ok"
