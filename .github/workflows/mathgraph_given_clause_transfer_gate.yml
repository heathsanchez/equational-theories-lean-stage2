name: MathGraph Given Clause Transfer Gate
on:
  pull_request:
    types: [opened, reopened, synchronize, edited]
  workflow_dispatch:
jobs:
  transfer:
    if: github.event_name == 'workflow_dispatch' || (github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.title == '[MathGraph Trigger] Given Clause Saturation Gate')
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: mathgraph/developmental-continuation-20260820
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install 'datasets>=3,<5'
      - name: Run source-distinct transfer gate
        run: python experiments/mathgraph/run_given_clause_transfer_gate.py
      - uses: actions/upload-artifact@v4
        with:
          name: mathgraph-given-clause-transfer-gate
          path: experiments/mathgraph/results/given-clause-transfer-gate.json
          if-no-files-found: error
          retention-days: 30
