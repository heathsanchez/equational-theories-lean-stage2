name: MathGraph Normal0040 Distance2-to1 Gate

on:
  push:
    branches:
      - mathgraph/binary-fusion-isolated-20260821
    paths:
      - 'experiments/mathgraph/NORMAL0040_DISTANCE2_TO1_FREEZE_20260821.json'
      - 'experiments/mathgraph/run_normal0040_distance2_to1_gate.py'
      - '.github/workflows/mathgraph_normal0040_distance2_to1_gate.yml'
  pull_request:
    paths:
      - 'experiments/mathgraph/NORMAL0040_DISTANCE2_TO1_FREEZE_20260821.json'
      - 'experiments/mathgraph/run_normal0040_distance2_to1_gate.py'
      - '.github/workflows/mathgraph_normal0040_distance2_to1_gate.yml'

concurrency:
  group: mathgraph-normal0040-distance2-to1-${{ github.ref }}
  cancel-in-progress: true

jobs:
  distance2-to1:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install 'datasets>=3,<5'
      - name: Run frozen distance-2 residual contraction gate
        run: python experiments/mathgraph/run_normal0040_distance2_to1_gate.py
      - uses: actions/upload-artifact@v4
        with:
          name: mathgraph-normal0040-distance2-to1
          path: experiments/mathgraph/results/normal0040-distance2-to1-gate.json
