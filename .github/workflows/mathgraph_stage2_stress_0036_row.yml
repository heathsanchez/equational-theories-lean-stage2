name: MathGraph fresh stress residual rows

on:
  push:
    branches: [mathgraph/stress-0036-residual-20260831]
    paths:
      - '.github/workflows/mathgraph_stage2_stress_0036_row.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  row:
    runs-on: ubuntu-24.04
    steps:
      - name: Fetch and print exact fresh residuals
        run: |
          set -euo pipefail
          curl -fL --retry 4 \
            'https://huggingface.co/datasets/SAIRfoundation/equational-theories-selected-problems/resolve/main/data/stage2_stress_test.jsonl' \
            -o /tmp/stress.jsonl
          python - <<'PY'
          import json
          rows=[json.loads(x) for x in open('/tmp/stress.jsonl')]
          for rid in ('order5_normal_0030','order5_normal_0036'):
              r=next(r for r in rows if r['id']==rid)
              print('FRESH_RESIDUAL_ROW '+json.dumps(r,sort_keys=True),flush=True)
          PY
