name: MathGraph Provenance Invariant Extraction
on:
  pull_request:
    branches: [mathgraph/residual-conditioned-operator-induction-20260821]
  workflow_dispatch:
jobs:
  provenance-invariant:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install 'datasets>=3,<5'
      - name: Run provenance-level invariant extraction
        run: python experiments/mathgraph/run_provenance_invariant_extraction.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mathgraph-provenance-invariant-extraction
          path: experiments/mathgraph/results/provenance-invariant-extraction.json
          if-no-files-found: warn
          retention-days: 30
